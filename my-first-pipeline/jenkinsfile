pipeline {
  agent {
    docker { image 'node:20-alpine3.17'
             image 'nginx:latest'  }
  }
  stages {
    stage('version') {
      steps {
        sh 'node --version'
      }
    }
    stage('nginx-version') {
      steps {
        sh 'nginx -v'
      }
    }
  }
}